# SimpleTron
SimpleTron is a Java-based virtual machine that simulates a simple computer. It uses 100 memory slots and executes 4-digit instructions where the first two digits are the opcode and the last two are a memory address. It supports input/output, arithmetic, branching, and memory operations, demonstrating basic CPU execution and low-level concepts.
